server:
  port: 8083

plazoleta:
  service:
    url: http://localhost:8080

spring:
  application:
    name: ms-trazabilidad
  data:
    mongodb:
      uri: ${MONGODB_URI}

appDescription: "Pragma Ms-Trazabilidad"
appVersion: "1.0.0"